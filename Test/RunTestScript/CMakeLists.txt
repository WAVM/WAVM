if(ENABLE_RUNTIME)
	add_executable(RunTestScript RunTestScript.cpp)
	target_link_libraries(RunTestScript Logging IR WASTParse Runtime ThreadTest)
	set_target_properties(RunTestScript PROPERTIES FOLDER Testing)
endif()